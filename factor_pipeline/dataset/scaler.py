from __future__ import annotations

import pandas as pd


def scale_dataset(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder dataset scaler.

    Args:
        df: Built factor dataset.

    Returns:
        The same DataFrame without modification.
    """
    return df
