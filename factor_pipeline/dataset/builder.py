from __future__ import annotations

import pandas as pd


def build_dataset(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder dataset builder.

    Args:
        df: Labeled factor DataFrame.

    Returns:
        The same DataFrame without modification.
    """
    return df
