from __future__ import annotations

import pandas as pd


def split_dataset(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder dataset splitter.

    Args:
        df: Scaled factor dataset.

    Returns:
        The same DataFrame without modification.
    """
    return df
